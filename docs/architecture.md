# nemo-cli 架构文档

> 生成日期：2025-12-21
> 扫描级别：快速扫描（Quick Scan）

---

## 📋 执行摘要

**nemo-cli** 是一个 TypeScript CLI 工具集 monorepo，旨在简化日常开发中的 Git 操作、文件处理、包管理和 AI 集成。采用 pnpm workspaces 管理多个独立发布的包，共享核心工具库，使用 React Ink 构建终端 UI。

---

## 🏗️ 系统架构

### 架构模式

| 模式 | 说明 |
|------|------|
| **Monorepo** | 单仓库管理多个独立发布的 npm 包 |
| **分层架构** | CLI命令层 → 服务层 → 工具层 |
| **共享核心** | @nemo-cli/shared 作为所有包的基础依赖 |
| **命令模式** | 使用 Commander.js 实现命令注册和分发 |
| **组件化TUI** | React Ink 构建可复用的终端组件 |

### 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户终端                                   │
└─────────────────────────────────────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
        ▼                       ▼                       ▼
   ┌─────────┐            ┌─────────┐            ┌─────────┐
   │   ng    │            │   nf    │            │   np    │
   │  (git)  │            │ (file)  │            │(package)│
   └────┬────┘            └────┬────┘            └────┬────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      @nemo-cli/shared                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ command  │ │ prompts  │ │git-handle│ │   file   │ ...        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       @nemo-cli/ui                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ BigText  │ │ Message  │ │   List   │ │ Provider │            │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     外部依赖/服务                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │   Git    │ │ Node.js  │ │Confluence│ │ OpenAI   │            │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 包职责

### @nemo-cli/git（CLI入口：`ng`）

**职责**：简化日常 Git 操作，提供交互式界面

| 命令 | 功能 | 特性 |
|------|------|------|
| `ng commit` | 交互式提交 | 自动检测staged文件、读取commitlint配置、提取ticket号 |
| `ng checkout` | 分支切换 | 自动stash/pop、支持远程分支 |
| `ng branch clean` | 清理分支 | 按时间过滤、批量删除 |
| `ng pull/push` | 拉取/推送 | 简化操作 |
| `ng stash` | 暂存管理 | 交互式选择 |
| `ng merge` | 分支合并 | 交互式选择 |
| `ng diff` | 差异查看 | 本地/远程对比 |

### @nemo-cli/ai（CLI入口：`na`）

**职责**：AI功能集成，MCP服务器

| 服务 | 功能 | 集成 |
|------|------|------|
| Confluence | 创建/获取文档 | Atlassian API |
| Mail | 发送邮件 | SMTP/Gmail |
| Slack | Bot集成 | Slack API |
| Swagger | API解析 | OpenAPI |
| Chat | AI对话 | OpenAI/DeepSeek/Google |

### @nemo-cli/file（CLI入口：`nf`）

**职责**：文件和AST操作

| 命令 | 功能 |
|------|------|
| `nf ast` | AST分析和转换 |
| `nf clean` | 清理文件 |
| `nf create-routes` | 路由生成 |
| `nf delete` | 删除文件 |
| `nf list` | 列出文件 |

### @nemo-cli/package（CLI入口：`np`）

**职责**：pnpm工作区管理

| 命令 | 功能 |
|------|------|
| `np add` | 添加依赖 |
| `np remove` | 移除依赖 |
| `np upgrade` | 升级依赖 |
| `np clean` | 清理依赖 |
| `np list` | 列出依赖 |

### @nemo-cli/shared

**职责**：共享工具库，所有CLI包的基础依赖

| 模块 | 功能 | 主要导出 |
|------|------|----------|
| `command` | 命令执行 | exec, spawn |
| `prompts` | 交互式提示 | confirm, select, input |
| `git-handle` | Git操作 | status, branch, commit |
| `file` | 文件操作 | read, write, delete |
| `config` | 配置管理 | load, save |
| `spinner` | 加载动画 | start, stop, succeed |
| `log` | 日志输出 | info, warn, error |

### @nemo-cli/ui

**职责**：React TUI组件库

| 组件 | 功能 |
|------|------|
| `BigText` | 大字体文本显示（figlet） |
| `Message` | 消息提示（成功/错误/警告） |
| `List` | 列表显示 |
| `ProcessMessage` | 进度消息 |
| `Provider` | Context提供者 |

### @nemo-cli/mail

**职责**：邮件服务

| 模块 | 功能 |
|------|------|
| `send` | 邮件发送 |
| `release.tsx` | 发布邮件模板 |
| `data.tsx` | 数据邮件模板 |

---

## 🛠️ 技术栈

### 核心技术

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **语言** | TypeScript | ^5.9.3 | 类型安全 |
| **运行时** | Node.js | ^20.19.0/>=22.12.0 | ESM支持 |
| **包管理** | pnpm | latest | Monorepo管理 |
| **构建** | Rolldown | 1.0.0-beta.52 | 快速构建 |
| **类型** | rolldown-plugin-dts | ^0.18.1 | .d.ts生成 |

### CLI框架

| 技术 | 用途 |
|------|------|
| Commander.js | 命令行框架 |
| @clack/prompts | 交互式提示 |
| Ink | React终端渲染 |
| chalk | 终端样式 |
| ora | 加载动画 |
| figlet | ASCII艺术字 |

### AI/API集成

| 技术 | 用途 |
|------|------|
| @ai-sdk/* | OpenAI/DeepSeek/Google集成 |
| fastmcp | MCP服务器框架 |
| confluence.js | Confluence API |
| nodemailer | 邮件发送 |
| @slack/bolt | Slack Bot |

### 开发工具

| 技术 | 用途 |
|------|------|
| Biome | Lint + Format |
| Vitest | 单元测试 |
| Husky | Git Hooks |
| Commitlint | 提交规范 |
| lint-staged | 暂存检查 |
| Knip | 死代码检测 |

---

## 🔄 数据流

### Git Commit 流程

```
用户执行 ng commit
       │
       ▼
┌──────────────────┐
│ 检测staged/unstaged│
│     文件         │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 读取commitlint   │
│    配置         │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 交互式选择       │
│ type/scope/msg   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 执行 lint-staged │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  git commit      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 询问是否push     │
└──────────────────┘
```

### MCP服务调用流程

```
AI Agent (Cursor/Claude)
         │
         ▼
┌──────────────────┐
│   MCP Protocol   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ @nemo-cli/ai     │
│  MCP Server      │
└────────┬─────────┘
         │
    ┌────┴────┬────────────┐
    │         │            │
    ▼         ▼            ▼
┌───────┐ ┌───────┐  ┌───────────┐
│Confl. │ │ Mail  │  │  Slack    │
│ API   │ │ SMTP  │  │   API     │
└───────┘ └───────┘  └───────────┘
```

---

## 📁 配置管理

### 环境变量（.env）

```bash
# Confluence
CONFLUENCE_URL=https://xxx.atlassian.net
CONFLUENCE_EMAIL=xxx@xxx.com
CONFLUENCE_TOKEN=xxx

# Google/Gmail
GOOGLE_APP_PASSWORD=xxx

# AI
OPENAI_API_KEY=xxx
DEEPSEEK_API_KEY=xxx
GOOGLE_API_KEY=xxx

# Slack
SLACK_BOT_TOKEN=xxx
SLACK_SIGNING_SECRET=xxx
```

### 包配置文件

| 文件 | 用途 |
|------|------|
| `tsconfig.json` | TypeScript配置 |
| `biome.json` | Biome lint/format |
| `commitlint.config.mjs` | 提交规范 |
| `vitest.config.ts` | 测试配置 |
| `rolldown.config.ts` | 构建配置 |

---

## 🔐 安全考虑

| 方面 | 措施 |
|------|------|
| **敏感信息** | 使用环境变量，不提交.env |
| **API Token** | Confluence/Google Token通过.env配置 |
| **Git Hooks** | Husky确保提交规范和代码检查 |
| **依赖安全** | pnpm lockfile锁定版本 |

---

## 📊 扩展性设计

### 添加新CLI命令

1. 在对应包的 `src/commands/` 创建新命令文件
2. 在 `src/index.ts` 注册命令
3. 使用 `@nemo-cli/shared` 的工具函数
4. 使用 `@nemo-cli/ui` 的组件显示

### 添加新包

1. 在 `packages/` 创建新目录
2. 复制现有包的 `package.json`、`rolldown.config.ts`、`tsconfig.build.json`
3. 添加 `@nemo-cli/shared` 依赖
4. 在根 `commitlint.config.mjs` 添加新scope

### 添加新MCP工具

1. 在 `packages/ai/src/services/` 创建新服务目录
2. 创建 `mcp.ts` 注册工具
3. 在 `src/index.ts` 导入并注册

---

## 🎯 设计决策

| 决策 | 原因 |
|------|------|
| **pnpm workspace** | 高效磁盘空间利用，严格依赖管理 |
| **Rolldown** | Rust构建工具，比esbuild/tsup更快 |
| **Biome** | 替代ESLint+Prettier，更快更统一 |
| **ESM only** | 现代Node.js标准，更好的tree-shaking |
| **React Ink** | 复用React生态，声明式TUI |
| **@clack/prompts** | 美观的交互式提示 |
| **Commander.js** | 成熟稳定的CLI框架 |
