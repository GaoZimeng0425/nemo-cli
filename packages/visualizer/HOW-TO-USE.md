# ✅ 正确! `nd visualize --open` 是正确的用法!

## 🎯 完整使用流程

### 在你的 Next.js 项目中使用

假设你在 `/path/to/your-nextjs-app` 目录:

```bash
# 1. 分析项目依赖 (如果还没做)
cd /path/to/your-nextjs-app
nd analyze --format ai

# 这会生成 ai-docs/deps.ai.json 文件

# 2. 启动可视化工具
nd visualize --open

# 浏览器会自动打开 http://localhost:3000

# 3. 在可视化界面中:
#    - 拖拽 ai-docs/deps.ai.json 文件到页面
#    - 或点击选择该文件

# 完成! 开始探索你的依赖关系图 🎉
```

## 📋 命令选项

```bash
nd visualize [options]

Options:
  -p, --port <number>  指定端口 (默认: 3000)
  --open               自动打开浏览器
  -h, --help           显示帮助信息
```

### 示例

```bash
# 使用默认端口 (3000) 并打开浏览器
nd visualize --open

# 使用自定义端口
nd visualize --port 8080 --open

# 不自动打开浏览器
nd visualize
```

## 🎨 功能介绍

### 1. **依赖关系图可视化**
   - 🖱️ 拖拽移动节点
   - 🔍 滚轮缩放
   - 📏 平移画布

### 2. **筛选和搜索**
   - 🎨 按 Scope 过滤 (App/Workspace/External)
   - 📄 页面视图 (查看特定路由的依赖)
   - 🔁 循环视图 (查看 SCC 循环依赖)
   - 🔍 搜索节点 (输入文件名)

### 3. **节点详情**
   - 📁 文件路径
   - 📊 依赖/被依赖统计
   - 📦 包信息
   - 🔗 循环依赖信息

## 🎨 节点颜色说明

- 🔵 **蓝色** = App (应用代码)
- 🟢 **绿色** = Workspace (workspace 包)
- ⚫ **灰色** = External (外部依赖 npm 包)
- 🟠 **橙色** = Internal (内部依赖)
- 🟣 **紫色** = Other (其他)

## ⚠️ 注意事项

### 首次使用

1. **确保已安装依赖**
   ```bash
   cd /path/to/nemo-cli
   pnpm install
   ```

2. **确保已分析项目**
   ```bash
   cd /path/to/your-nextjs-app
   nd analyze --format ai
   ```

3. **启动可视化工具**
   ```bash
   nd visualize --open
   ```

### 如果遇到错误

#### 错误: "Visualizer package not found"
```bash
# 确保在 nemo-cli 根目录
cd /path/to/nemo-cli

# 安装依赖
pnpm install
```

#### 错误: "端口被占用"
```bash
# 使用其他端口
nd visualize --port 3001 --open
```

#### 错误: "找不到 deps.ai.json"
```bash
# 确保在正确的项目目录
cd /path/to/your-nextjs-app

# 重新生成依赖数据
nd analyze --format ai
```

## 🔧 故障排除

### 问题: 浏览器没有自动打开

手动访问: `http://localhost:3000`

### 问题: 页面空白

1. 检查浏览器控制台是否有错误
2. 确保已加载 `deps.ai.json` 文件
3. 尝试刷新页面

### 问题: 找不到文件

文件应该在: `ai-docs/deps.ai.json`

如果不在:
```bash
# 查看文件位置
find . -name "deps.ai.json"

# 或重新生成
nd analyze --format ai
```

## 📚 相关文档

- **详细使用指南**: `packages/visualizer/USAGE.md`
- **快速开始**: `packages/visualizer/START-HERE.md`
- **项目说明**: `packages/visualizer/README.md`

## 🎉 开始使用

现在就试试吧:

```bash
cd /path/to/your-nextjs-app
nd analyze --format ai
nd visualize --open
```

**享受可视化你的项目依赖关系!** 🚀
