# `@nemo-cli/package`

> @nemo-cli/package Make pnpm Workspace Operation Easier

## Install
```
$ pnpm add @nemo-cli/package --global
```

## Usage

```bash
np add [...packages]
np add -D -E
np add [...packages] -S -E

np remove

np upgrade
```
