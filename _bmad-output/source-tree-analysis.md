# nemo-cli 源码树分析

> 生成日期：2025-12-21
> 扫描级别：快速扫描（Quick Scan）

---

## 📁 项目根目录结构

```
nemo-cli/
├── packages/                    # 🎯 核心包目录（pnpm workspace）
│   ├── git/                     # Git操作CLI（ng命令）
│   ├── ai/                      # AI CLI + MCP服务器（na命令）
│   ├── file/                    # 文件AST操作CLI（nf命令）
│   ├── package/                 # pnpm工作区管理CLI（np命令）
│   ├── shared/                  # 共享工具库（核心依赖）
│   ├── ui/                      # React TUI组件库（Ink）
│   ├── mail/                    # 邮件服务（React Email）
│   └── global.d.ts              # 全局类型声明
│
├── aicommits/                   # 📚 参考项目：AI提交消息生成
├── better-commits/              # 📚 参考项目：约定式提交CLI
├── gitpick/                     # 📚 参考项目：Git仓库脚手架
├── taze/                        # 📚 参考项目：依赖更新检查
│
├── docs/                        # 📄 文档输出目录
│   ├── packages-git/            # git包详细文档（已生成）
│   └── bmm-workflow-status.yaml # BMAD工作流状态
│
├── _bmad/                       # 🤖 BMAD配置目录
│   ├── bmm/                     # BMM模块
│   ├── bmb/                     # BMB模块
│   ├── cis/                     # CIS模块
│   └── core/                    # 核心模块
│
├── dist/                        # 构建输出
├── node_modules/                # 依赖
│
├── package.json                 # 根package.json
├── pnpm-workspace.yaml          # pnpm工作区配置
├── pnpm-lock.yaml               # 依赖锁定
├── tsconfig.json                # TypeScript配置
├── tsconfig.build.json          # 构建TypeScript配置
├── biome.json                   # Biome配置（lint + format）
├── commitlint.config.mjs        # Commitlint配置
├── vitest.config.ts             # Vitest测试配置
├── knip.json                    # Knip死代码检测配置
├── rolldown.config.ts           # Rolldown基础配置
├── sea-config.json              # Node SEA配置
└── README.md                    # 项目README
```

---

## 📦 核心包详细结构

### @nemo-cli/git（Git操作CLI）

```
packages/git/
├── bin/
│   └── index.mjs                # CLI入口点（ng命令）
├── src/
│   ├── index.ts                 # 主入口，命令注册
│   ├── utils.ts                 # Git操作工具函数
│   ├── constants/
│   │   ├── index.ts             # 帮助信息常量
│   │   └── stash.ts             # Stash相关常量
│   └── commands/
│       ├── branch.ts            # 分支管理（delete, clean）
│       ├── checkout.ts          # 分支切换（自动stash）
│       ├── commit.ts            # 交互式提交
│       ├── commit-options.ts    # Commitlint配置读取
│       ├── diff.ts              # 差异查看
│       ├── list.ts              # 分支列表
│       ├── merge.ts             # 分支合并
│       ├── pull.ts              # 拉取远程
│       ├── push.ts              # 推送远程
│       └── stash.ts             # 暂存管理
├── dist/                        # 构建输出
├── package.json
├── rolldown.config.ts
└── tsconfig.build.json
```

### @nemo-cli/ai（AI CLI + MCP服务器）

```
packages/ai/
├── bin/
│   └── index.mjs                # CLI入口点（na命令）
├── src/
│   ├── index.ts                 # 主入口，MCP服务器启动
│   ├── components/              # React组件（预留）
│   ├── hooks/                   # React Hooks（预留）
│   └── services/
│       ├── chat/
│       │   └── index.ts         # AI聊天服务
│       ├── confluence/
│       │   ├── index.ts         # Confluence API入口
│       │   ├── createContent.ts # 创建文档
│       │   ├── getContent.ts    # 获取文档
│       │   ├── mcp.ts           # MCP工具注册
│       │   └── template.ts      # 文档模板
│       ├── mails/
│       │   └── mcp.ts           # 邮件MCP工具
│       ├── slack/
│       │   ├── bot.ts           # Slack Bot
│       │   └── tools/           # Slack工具
│       │       ├── getPRD.ts
│       │       ├── sendEmail.ts
│       │       └── index.ts
│       └── swagger/
│           ├── index.ts         # Swagger解析
│           ├── loop.ts          # 循环处理
│           ├── mcp.ts           # MCP工具
│           └── type.ts          # 类型定义
├── manifest.json                # MCP清单
├── dist/
├── package.json
└── rolldown.config.ts
```

### @nemo-cli/file（文件AST操作CLI）

```
packages/file/
├── bin/
│   └── index.mjs                # CLI入口点（nf命令）
├── src/
│   ├── index.ts                 # 主入口，命令注册
│   ├── constants.ts             # 常量定义
│   └── commands/
│       ├── ast/
│       │   ├── index.ts         # AST命令入口
│       │   ├── ast-handle.ts    # AST处理核心
│       │   ├── create-table.tsx # 表格生成（React）
│       │   └── slack.ts         # Slack集成
│       ├── clean.ts             # 清理命令
│       ├── create-routes.ts     # 路由生成
│       ├── delete.ts            # 删除命令
│       └── list.ts              # 列表命令
├── dist/
├── package.json
└── rolldown.config.ts
```

### @nemo-cli/package（pnpm工作区管理CLI）

```
packages/package/
├── bin/
│   └── index.mjs                # CLI入口点（np命令）
├── src/
│   ├── index.ts                 # 主入口，命令注册
│   ├── constants.ts             # 常量定义
│   └── commands/
│       ├── add.ts               # 添加依赖
│       ├── clean.ts             # 清理依赖
│       ├── list.ts              # 列表依赖
│       ├── remove.ts            # 移除依赖
│       └── upgrade.ts           # 升级依赖
├── dist/
├── package.json
└── rolldown.config.ts
```

### @nemo-cli/shared（共享工具库）

```
packages/shared/
├── src/
│   ├── index.ts                 # 主入口，导出所有工具
│   ├── constants.ts             # 共享常量
│   └── utils/
│       ├── browser.ts           # 浏览器操作
│       ├── color.ts             # 颜色处理
│       ├── command.ts           # 命令执行
│       ├── common.ts            # 通用工具
│       ├── config.ts            # 配置管理
│       ├── dynamic-command-examples.ts  # 动态命令示例
│       ├── env.ts               # 环境变量
│       ├── error.ts             # 错误处理
│       ├── file.ts              # 文件操作
│       ├── format.ts            # 格式化
│       ├── git-handle/
│       │   └── index.ts         # Git操作封装
│       ├── log.ts               # 日志
│       ├── npminfo.ts           # NPM信息
│       ├── packageJson.ts       # package.json操作
│       ├── pathname.ts          # 路径处理
│       ├── promise.ts           # Promise工具
│       ├── prompts.ts           # 交互式提示
│       ├── spinner.ts           # 加载动画
│       ├── types.ts             # 类型定义
│       └── workspace.ts         # 工作区操作
├── types/
│   ├── execa.d.ts
│   ├── tinyexec.d.ts
│   └── zx.d.ts
├── dist/
├── package.json
└── rolldown.config.ts
```

### @nemo-cli/ui（React TUI组件库）

```
packages/ui/
├── src/
│   ├── index.ts                 # 主入口
│   ├── components/
│   │   ├── index.ts             # 组件导出
│   │   ├── big-text.tsx         # 大字体文本（figlet）
│   │   ├── list.tsx             # 列表组件
│   │   ├── message.tsx          # 消息组件
│   │   ├── process-message.tsx  # 进度消息
│   │   └── provider/
│   │       └── index.tsx        # Context Provider
│   └── hooks/
│       └── useTerminalSize.ts   # 终端尺寸Hook
├── dist/
├── package.json
└── rolldown.config.ts
```

### @nemo-cli/mail（邮件服务）

```
packages/mail/
├── src/
│   ├── index.ts                 # 主入口
│   └── services/
│       └── send.ts              # 邮件发送服务
├── emails/
│   ├── data.tsx                 # 数据邮件模板
│   └── release.tsx              # 发布邮件模板
├── dist/
├── package.json
└── rolldown.config.ts
```

---

## 🔗 包依赖关系图

```
                    ┌─────────────────┐
                    │  @nemo-cli/ui   │ ← React TUI (Ink)
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
     ┌────────────┐  ┌────────────┐  ┌────────────┐
     │ @nemo-cli/ │  │ @nemo-cli/ │  │ @nemo-cli/ │
     │    git     │  │   file     │  │  package   │
     └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
           │               │               │
           └───────────────┼───────────────┘
                           │
                           ▼
                 ┌─────────────────┐
                 │ @nemo-cli/shared│ ← 核心工具库
                 └────────┬────────┘
                          │
           ┌──────────────┴──────────────┐
           │                             │
           ▼                             ▼
    ┌────────────┐               ┌────────────┐
    │ @nemo-cli/ │               │ @nemo-cli/ │
    │    mail    │               │     ai     │
    └────────────┘               └────────────┘
           │                             │
           └──────────────┬──────────────┘
                          │
                          ▼
                   ┌────────────┐
                   │  @nemo-cli │
                   │    /git    │
                   └────────────┘
```

---

## 📂 关键目录说明

| 目录 | 用途 | 关键文件 |
|------|------|----------|
| `packages/*/src/commands/` | CLI命令实现 | 每个命令一个文件 |
| `packages/*/src/services/` | 服务层 | API集成、业务逻辑 |
| `packages/shared/src/utils/` | 共享工具 | git-handle, prompts, file等 |
| `packages/ui/src/components/` | TUI组件 | big-text, message, list等 |
| `packages/*/bin/` | CLI入口点 | index.mjs |
| `packages/*/dist/` | 构建产物 | .js, .d.ts |

---

## 🎯 入口点

| 包 | 命令 | 入口文件 | 功能 |
|----|------|----------|------|
| @nemo-cli/git | `ng` | `packages/git/bin/index.mjs` | Git操作 |
| @nemo-cli/ai | `na` | `packages/ai/bin/index.mjs` | AI CLI |
| @nemo-cli/file | `nf` | `packages/file/bin/index.mjs` | 文件操作 |
| @nemo-cli/package | `np` | `packages/package/bin/index.mjs` | 包管理 |
